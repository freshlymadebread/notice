递归：
    好处：思路简洁
    坏处：容易栈溢出
解决方法：蹦床函数
         尾递归优化
        
蹦床函数是指，在function 的return处，返回自身的绑定调用，这样绝对不会栈溢出，因为一次返回就已经把上次的调用帧清空了。但是会增加堆的空间使用,因为返回了函数对象。
            蹦床函数只适用于crried化的函数
尾递归优化是ES6 严格模式下自动进行的代码优化，使用函数式编程对function进行封装。使用while循环代替。