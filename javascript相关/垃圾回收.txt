js 中有两种垃圾回收原理
    1. 标记-清除
        原理，可达性。从根对象开始进行迭代遍历，遍历一遍后，对没有标记的对象进行删除
    2. 引用计数
        每个对象有一个count，每多一个引用的对象，count就+1，如果count等于零，就说明没有被引用的了，就删除。
        引用计数容易造成内存泄漏：循环引用情况下 计数永远不会为零，但是不一定能被访问

内存泄漏的几种方式
    1. 循环引用
    2. 闭包的某些无用变量
    3. 在函数中声明的全局变量
    4. 被遗忘的计数器
    5. DOM元素
